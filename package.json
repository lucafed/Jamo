{
  "name": "jamo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Jamo datasets builder (regions, categories, offline POIs + offline events)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:events": "node scripts/build_events_all.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
